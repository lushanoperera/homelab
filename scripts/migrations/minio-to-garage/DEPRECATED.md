> **DEPRECATED 2026-04-10.**
>
> Superseded by `scripts/migrations/minio-to-rustfs/migrate.sh`. This script
> targets Garage, which was never deployed. Files are retained for reference
> only — see `docs/migrations/minio-to-rustfs.md` for the active runbook.
