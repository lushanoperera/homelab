# @defaultSensitive=false
# @defaultRequired=infer
# ---

# Grafana admin login. Resolves from Vaultwarden via rbw at runtime.
# @sensitive
GRAFANA_ADMIN_PASSWORD=exec(`rbw get grafana-admin-password --folder "Homelab/grafana"`)
