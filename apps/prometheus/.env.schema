# @defaultSensitive=false
# @defaultRequired=infer
# ---

# Prometheus has no secrets. Remote-write ingestion is unauthenticated
# on the internal LAN (bound to 192.168.100.100 only). All credentials
# for scraped exporters, if added later, should resolve via rbw here.
